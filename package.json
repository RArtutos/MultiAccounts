{
  "name": "proxy-streaming-manager",
  "version": "1.0.0",
  "description": "Sistema de gestión de cuentas de streaming con proxy reverso",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "proxy"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:proxy\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev:proxy": "cd proxy && npm run dev",
    "build": "npm run build:frontend && npm run build:backend && npm run build:proxy",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:proxy": "cd proxy && npm run build",
    "test": "npm run test:frontend && npm run test:backend && npm run test:proxy",
    "test:frontend": "cd frontend && npm run test",
    "test:backend": "cd backend && npm run test",
    "test:proxy": "cd proxy && npm run test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "prettier": "^3.1.0",
    "eslint": "^8.54.0",
    "eslint-config-prettier": "^9.0.0"
  }
}